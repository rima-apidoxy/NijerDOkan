/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "encrypted-tbn0.gstatic.com",
      "img4.dhresource.com",
      "example.com",
      "static.cilory.com",
      "via.placeholder.com",
      "placehold.co"
    ],
  },
};

export default nextConfig;
